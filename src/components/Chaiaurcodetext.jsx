import React from "react";

function Chaiaurcodetext({ className }) {
  return (
    <div
      className={`${className} text-[80px] font-bold text-center w-full`}
      style={{ textShadow : "2px 2px 0px rgba(0, 0, 0, 0.25)" }}
    >
      Chai aur Code
    </div>
  );
}

export default Chaiaurcodetext;
